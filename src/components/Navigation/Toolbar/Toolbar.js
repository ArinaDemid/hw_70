import React from 'react';
import Logo from '../../Logo/Logo';

const Toolbar = () => (
  <header className="Toolbar" style={{backgroundColor: '#397ce2', paddingLeft: '20px', paddingBottom: '8px'}}>
    <Logo />
  </header>
);

export default Toolbar;